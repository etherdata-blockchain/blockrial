---
sidebar_position: 6
title: 创建钱包、挖矿、转账、查询余额
---

本教程将会介绍如何使用Etherdata-SDK 签署交易。请建立四个python文件（.py）

## 创建账户

```python
from etherdata_sdk.account import Account

account = Account()

# 通过助记词（二选一）
# mnemonic=""
# account.create_private_key_from_mnemonic(mnemonic)

# 创建随机的account（二选一）
account.create_random_private_key()

# 保存key
account.save(output_file="./private3.key")
```

## 挖矿


```python

from etherdata_sdk.account import Account
from etherdata_sdk.json_rpc.miner import Miner

account = Account()
# 读取key
account.read_private_key_from_file("private3.key")

from_account = account

# 挖矿获取初始货币（连接盒子的地址）
miner = Miner(url="http://")
miner.set_etherbase(from_account.address)
miner.start()

```

 
## 交易

```python
from etherdata_sdk import Transaction
from etherdata_sdk.account import Account

account = Account()
# 读取key
account.read_private_key_from_file("private3.key")

from_account = account
to_account = Account().create_random_private_key()

transaction = Transaction(to=to_account.address, value=30000, gas=1000, gas_price=300, nonce=1, chain_id=3101)

signed = from_account.sign_transaction(transaction)

```

## 查询余额

```python
from etherdata_sdk.account import Account
from etherdata_sdk.json_rpc import JsonRpcMethods

account = Account()
# 读取key
account.read_private_key_from_file("private3.key")

from_account = account

client = JsonRpcMethods("https://rpc.etdchain.net")
balance = client.get_balance(address=from_account.address, tag="latest")
print(balance)
```
